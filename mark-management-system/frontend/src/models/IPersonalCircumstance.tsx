export interface IPersonalCircumstance {
  reg_no: string;
  details: string;
  semester: string;
  cat: number;
  comments: string;
}

export interface IPersonalCircumstanceRow {
  reg_no: string;
  name: string;
  programme: string;
  year: number;
  reg_status: string;
  cert_type: string;
  personal_circumstance_details: string;
  sem: string;
  cat: number;
  comments: string;
}
